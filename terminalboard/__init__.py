"""terminalboard — a pure-terminal TensorBoard viewer.

Reads TensorBoard event logs and renders them live inside any terminal (local or
SSH) as Unicode/braille text — scalars as curves, text summaries, and histograms
as heatmaps. No browser, no X11, no port forwarding.
"""

__version__ = "0.1.3"
